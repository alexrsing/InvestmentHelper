# PriceData Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# AWS Configuration
# Get your AWS credentials from the AWS Console:
# https://console.aws.amazon.com/iam/home#/security_credentials
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1

# Secrets Manager (Lambda only — not needed for local dev)
# In Lambda, all API keys and tiers are loaded from a single JSON secret.
# Set this env var in the Lambda function configuration:
# PRICE_FETCHER_SECRET_NAME=dev/price-fetcher/config
#
# For local development, set keys and tiers directly below.

# Data Source Configuration
# Options: auto, yfinance, alphavantage, twelvedata, finnhub, fmp
# - auto: Try yfinance first, fall back to paid sources (default)
# - yfinance: Only use Yahoo Finance (free, no API key, but rate limited)
# - alphavantage: Only use Alpha Vantage (requires API key)
# - twelvedata: Only use Twelve Data (requires API key)
# - finnhub: Only use Finnhub (requires API key)
# - fmp: Only use Financial Modeling Prep (requires API key)
DATA_SOURCE=auto

# Alpha Vantage Configuration (optional)
# Get your free API key from: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your_api_key_here
# Tier options: free, paid_30, paid_75, paid_150, paid_300
# Free tier: 25 requests/day, 5 requests/minute (no intraday)
ALPHA_VANTAGE_TIER=free

# Twelve Data Configuration (optional)
# Get your free API key from: https://twelvedata.com/account
TWELVEDATA_API_KEY=your_api_key_here
# Tier options: free, grow, pro, enterprise
# Free tier: 8 credits/minute, 800 credits/day (1 credit per request)
TWELVEDATA_TIER=free

# Finnhub Configuration (optional)
# Get your free API key from: https://finnhub.io/
FINNHUB_API_KEY=your_api_key_here
# Tier options: free, paid
# Free tier: 60 calls/minute
FINNHUB_TIER=free

# Financial Modeling Prep Configuration (optional)
# Get your free API key from: https://site.financialmodelingprep.com/developer
FMP_API_KEY=your_api_key_here
# Tier options: free, starter, premium, ultimate
# Free tier: 250 calls/day
FMP_TIER=free

# Price Fetching Optimization
# Skip symbols updated within this many minutes (default: 15)
STALENESS_THRESHOLD_MINUTES=15
# Maximum symbols to fetch per run (leave empty for unlimited)
MAX_SYMBOLS_PER_RUN=
# Force refresh all symbols, ignoring staleness threshold (true/false)
FORCE_REFRESH=false
